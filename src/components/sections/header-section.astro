---
interface Props {
  label?: string;
  title: string;
  subtitle?: string;
}

const { label, title, subtitle } = Astro.props;
---

<div class="flex flex-col items-center text-center">
  {
    label ? (
      <div class="text-neutral-300 mb-4 font-medium text-xs uppercase tracking-widest">{label}</div>
    ) : null
  }
  <h2 class="font-heading text-3xl md:text-4xl lg:text-[40px]">
    {title}
  </h2>
  {
    subtitle ? (
      <p class="mt-6 text-balance text-sm text-muted-foreground">{subtitle}</p>
    ) : null
  }
</div>
